from setuptools import setup

setup(
    name='drpruning',
    packages=["drpruning"],
    version='0.1',
    description='DRPruning',
    author='Anonymous',
    entry_points={
        "console_scripts": [],
    },
    package_data={},
    classifiers=["Programming Language :: Python :: 3"],
)

# auto-gptq cannot be installed